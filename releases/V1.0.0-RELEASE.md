# Release Record — Fleet Utilization Reports MVP v1.0.0

> Status: **PREPARED** (not tagged; not pushed)
> Date prepared: 2026-08-03
> Proposed tag: **`v1.0.0`** (annotated; not yet created)
> Release commit: **`390c838d52f97995082e6746f299fb621bf4d26d`**
> Commit message: `test: complete PACK-005 evidence closure`
> Branch: `master`

## 1. Release intent

Mark the accepted locked MVP after PACK-001…PACK-005 for remote backup and release marking.
This record does **not** authorize new product features, migrations, Frotcom, n8n, reports UI, or export modules.

## 2. Included MVP scope

- Authentication and role handling (`app_metadata.role`: admin / manager / viewer)
- Master-data workflows (vehicles, drivers, customers)
- Assignments (overlap guard, in-place correction, no hard delete)
- Excel assignment import: preview, validation, confirmation
- Atomic per-row persistence (`persist_assignment_import_row`)
- Exact-duplicate and overlap handling
- Partial-success / completed_with_errors handling
- Audit preservation (`persistence_errors` separated from validation_*)
- Import error-report generation with formula-injection protection
- Live JWT/RLS evidence and remote DB persistence/rollback evidence (PACK-005)

## 3. Pack status

| Pack | Status | Checkpoint |
|---|---|---|
| PACK-001 | **ACCEPTED** | `20f2698` |
| PACK-002 | **ACCEPTED_WITH_FOLLOW_UPS** | `21ab8aa` |
| PACK-003 | **ACCEPTED_WITH_FOLLOW_UPS** | `a68d8f9` |
| PACK-004 | **ACCEPTED_WITH_FOLLOW_UPS** | `dbe59da` |
| PACK-005 | **ACCEPTED_WITH_FOLLOW_UPS** | `390c838` |

Follow-ups closed at PACK-005 (with residuals where noted): FU-002-01…06, FU-003-01…03, OQ-004-04.
See `sprints/sprint-005/ACCEPTANCE-RECORD.md`.

## 4. Validation summary (at PACK-005 acceptance / revalidated at preparation)

| Gate | Result |
|---|---|
| `npm test` | **63/63 PASS** (re-run at preparation) |
| `npm run lint` | **PASS** |
| `npm run build` | **PASS** |
| `git diff --check` | **PASS** |
| PACK-005 evidence suite | 37 cases · 36 PASS · 1 PARTIAL (C14) · 0 FAIL |
| Secret scan (release artifacts) | no credentials in this record |
| Working tree at release commit | clean |

## 5. Migration status

- Migrations in tree: **5** (through `20260730170000_pack004_import_hardening.sql`)
- Local == Remote: **PASS** at PACK-005 acceptance
- PACK-005 introduced **no** new migrations
- v1.0.0 release marking introduces **no** migrations

## 6. Accepted residuals

| ID | Residual |
|---|---|
| A / FU-002-05 | `correctAssignment` does not use `FOR UPDATE`; GiST exclusion remains authoritative |
| B / FU-003-02 | C14 remote transport-failure inject not performed; unit coverage retained |
| C / FU-002-06 | Local Docker daemon / local Supabase unavailable; remote non-prod evidence used |
| D | Non-production Auth test identities intentionally retained; credentials untracked (`.env.local`) |

## 7. Excluded future scope (not in v1.0.0)

- Frotcom live integration (blocked by DS-001)
- n8n daily sync workflows
- Reports / utilization dashboard UI
- Excel/PDF export modules beyond the accepted import error report
- Utilization mathematics UI
- Any unapproved future module / invented PACK-006

## 8. Deployment prerequisites

- Target environment variables configured (Supabase URL, anon/publishable key, server-side secrets via platform secrets — never commit)
- Supabase project migrations applied (Local == Remote for the five migrations)
- Auth users provisioned with roles in `app_metadata.role` only
- Non-production validation completed before any production cutover
- Confirm intended Git remote is configured (see §10) before push/tag publication

## 9. Rollback considerations

- Application: redeploy previous known-good commit / image; v1.0.0 tag points at `390c838`
- Database: forward-only migration policy; rollback requires explicit DBA plan (no automatic down-migrations assumed)
- Do not destroy production data to “roll back” evidence tooling
- Import jobs / assignments: operational restore from backups if required; product paths avoid hard DELETE of assignment history

## 10. Remote / tag publication gate (preparation finding)

At preparation time:

- Local tag `v1.0.0`: **does not exist**
- Remote tag check: **not verifiable** — `origin` is a placeholder URL (`github.com/DEIN-BENUTZERNAME/...`) and `git ls-remote` returns repository not found
- `master` has **no upstream**
- **Do not push or create the annotated tag** until `origin` points at the intended private repository and fast-forward safety is re-verified

Proposed annotated tag message (when authorized):

```text
Fleet Utilization Reports MVP v1.0.0

PACK-001 through PACK-005 completed.
Locked MVP accepted as production-ready with documented residuals.
```

## 11. Pilot-operation recommendation

Operate v1.0.0 as an **internal pilot** on the locked MVP scope:

1. Use a dedicated non-production (or carefully controlled production) Supabase project with migrations applied.
2. Limit users to admin / manager / viewer with correct `app_metadata.role`.
3. Exercise masters, assignments, and Excel import under real Auth sessions.
4. Keep residuals A–D visible to operators; do not treat FOR UPDATE gap or C14 unit residual as silent defects.
5. Do **not** enable Frotcom/n8n/reports/exports until separately authorized after DS-001 (and any future pack approval).
6. After remote is correctly configured: push `master`, create annotated `v1.0.0`, and publish this record in the same commit or an immediately following docs commit as authorized.

## 12. Explicit non-claims

- This preparation did **not** push, tag, or change product/migrations
- Local Docker database evidence is **not** claimed
- Frotcom / n8n / reports / exports are **not** part of v1.0.0
