# Architect Builder Workflow

Brainstorm → structured intake → Architect discovery → explicit GENERATE THE PACK gate → validated Architect Pack → Builder dry run → Architect conformance review → human approval → build → validation → Builder Report → Architect review → STATE and briefing update → Git checkpoint.

Architect never writes application code. Builder never edits before approval.
